PORT=5000
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/prathna_billing?schema=public"

# Generate a secure JWT secret with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# REQUIRED in production — the server will refuse to start without a valid secret.
JWT_SECRET=replace_this_with_a_64_char_random_hex_string
